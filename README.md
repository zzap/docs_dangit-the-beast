# docs_dangit-the-beast

## Usage

    # Install Composer dependencies
    composer install

    # Copy and customize the .ini file:
    cp config/config.ini.dist config/config.ini

    # List valid commands
    ./bin/docsdangit example
